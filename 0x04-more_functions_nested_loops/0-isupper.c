#include "main.h"
/**
 *_isuper -checks if a letter is uppercase
 *
 *@b: the number to be checked
 *
 * Return: 1 if c is uppercase 0 if otherwise
 */

int _isupper(int b);

{
int b;

if (b > 65 && b <= 90)
{
return (1);
}
return (0);
}
