#include "main.h"
/**
 *mul -multiplies two integers
 *
 * @a: the first number to be multiplied
 * @b: the second number to multiply the first
 *Return: the product of the two numbers multiplied
 */
int mul(int a, int b)
{
	return (a * b);
}
